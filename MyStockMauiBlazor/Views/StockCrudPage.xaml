<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyStockMauiBlazor.Views.NewTransactionPage"
             Title="New Transaction"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="LightSlateGray">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header area -->
            <RowDefinition Height="*"/>
            <!-- Form area -->
            <RowDefinition Height="Auto"/>
            <!-- Bottom tabs area -->
        </Grid.RowDefinitions>

        <!-- Header with username initial and title -->
        <Grid BackgroundColor="#3298ff" Padding="10" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Frame BackgroundColor="White"
                   CornerRadius="30"
                   HeightRequest="60"
                   WidthRequest="60"
                   HorizontalOptions="Start"
                   VerticalOptions="Center">
                <Label Text="U" HorizontalOptions="Center" VerticalOptions="Center" TextColor="#3298ff"/>
            </Frame>
            <Label Text="New Transaction" FontAttributes="Bold" FontSize="22" VerticalOptions="Center" Margin="20,0,0,0" TextColor="White" Grid.Column="1"/>
        </Grid>

        <!-- Form section -->
        <StackLayout Grid.Row="1" Padding="20">
            <Entry Placeholder="Ticker..." TextColor="Black" BackgroundColor="LightSlateGray" Margin="0,10" />
            <Entry Placeholder="Unit Count..." Keyboard="Numeric" TextColor="Black" BackgroundColor="LightSlateGray" Margin="0,10" />
            <Entry Placeholder="Unit Price..." Keyboard="Numeric" TextColor="Black" BackgroundColor="LightSlateGray" Margin="0,10" />
            <Button Text="Sell" BackgroundColor="OrangeRed" TextColor="White" Margin="0,10" />
            <Button Text="Buy" BackgroundColor="ForestGreen" TextColor="White" Margin="0,10" />
        </StackLayout>

        <!-- Bottom tabs for navigation -->
        <Grid Grid.Row="2" VerticalOptions="End" BackgroundColor="WhiteSmoke" HeightRequest="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Text="My Stocks" Grid.Column="0" BackgroundColor="Transparent" TextColor="#3298ff" FontAttributes="Bold"/>
            <BoxView BackgroundColor="#3298ff" WidthRequest="1" VerticalOptions="Fill" Grid.Column="0" HorizontalOptions="End" />
            <Button  Text="+" Grid.Column="1" BackgroundColor="Transparent" TextColor="#3298ff" FontSize="20" FontAttributes="Bold" />
            <BoxView BackgroundColor="#3298ff" WidthRequest="1" VerticalOptions="Fill" Grid.Column="1" HorizontalOptions="End" />
            <Button Text="Profile" Grid.Column="2" BackgroundColor="Transparent" TextColor="#3298ff" FontAttributes="Bold"/>
        </Grid>
    </Grid>
</ContentPage>